apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: rolloutapp
spec:
  replicas: 2
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: rolloutapp
  template:
    metadata:
      labels:
        app: rolloutapp
    spec:
      containers:
        - image: fleatbnp/express-microservice:5e3549d
          name: rolloutapp
          ports:
            - containerPort: 3000
  minReadySeconds: 30
  strategy:
    canary:
      steps:
        - setWeight: 50
        - pause: {}
